# Public site URL (used for metadata generation scripts if needed)
NEXT_PUBLIC_SITE_URL=https://lumenet-labs.netlify.app

# Optional analytics toggle (only if you later add consent-gated analytics)
NEXT_PUBLIC_ENABLE_ANALYTICS=false

# Placeholder server-only examples for future backend expansion.
# Do NOT use these in client-side code.
OPENAI_API_KEY=replace_me
DATABASE_URL=replace_me
